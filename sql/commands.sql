alter user 'root'@'localhost' identified with mysql_native_password by 'PASSWORD';

FLUSH PRIVILEGES;

ALTER USER root IDENTIFIED WITH mysql_native_password BY 'my-Pa$$word';

select * from photo;

delete from photo;

SELECT * from resolution;

